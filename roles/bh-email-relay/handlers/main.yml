
- name: postmap
  sudo: yes
  shell: postmap /etc/postfix/{{ item }}
  with_items:
    - transport
    - recipientmap
    - relay_recipients

- name: restart postfix
  sudo: yes
  service: name=postfix state=restarted

